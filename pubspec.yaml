name: todo_app
description: A Flutter Project for Testing riverPod with hooks

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  # ---------------- env packages ----------------- #
  flutter_dotenv: ^5.0.2
  # ---------------- Storage packages ----------------- #
  flutter_secure_storage: ^4.2.1
  # ---------------- State packages ----------------- #
  hooks_riverpod: ^0.14.0+4
  flutter_hooks: ^0.17.0
  get: ^4.3.8
  # ---------------- Hive packages ----------------- #
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  # ---------------- JSON packages ----------------- #
  json_serializable: ^5.0.0
  json_annotation: ^4.1.0
  # ---------------- freezed packages ----------------- #
  freezed_annotation: ^0.14.3
  intl: ^0.17.0
  flutter_svg: ^0.22.0
  getwidget: ^2.0.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  # ---------------- freezed packages ----------------- #
  freezed: ^0.14.5
  # ---------------- lints packages ----------------- #
  very_good_analysis: ^2.3.0
  flutter_lints: ^1.0.0
  # ---------------- generator packages ----------------- #
  build_runner: ^2.1.2
  hive_generator: ^1.1.1



flutter:
  uses-material-design: true
  assets:
  # ---------------- svg assets ----------------- #
    - lib/assets/svgs/

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700

